Hello <%= @user.first_name %>,

Your registration for the Annual International Dictyostelium Conference, 2012 was updated on <%= l @user.updated_at.to_date, :format => :long %> at <%= l @user.created_at, :format => :am_pm %>.

Following are your registration details:

	Name: <%= @user.first_name %> <%= @user.last_name%>
	Institute affiliation: <%= @user.institute %>

	Address: <%= @user.address %>, <%= @user.city %>, <%= @user.country %> <%= @user.zipcode %>
	Email: <%= @user.email %>
	Phone: <%= @user.phone %>
	VAT number/Tax exempt ID: <%= @user.billing_id%>

	Accommodation type: <%= @user.accommodation_type %>
	<% if Date.today <= Date.parse(Dicty11::Application.config.early_registration_deadline) %>
		<% if @user.accommodation_type == 'Single use' %>
			Early Registration: €830
		<% elsif @user.accommodation_type == 'Double use' %>
			Early Registration: €675 (per person)
		<% end %>
	<% elsif Date.today > Date.parse("5/31/2012") && Date.today <=  Date.parse("7/23/2012") %>
		<% if @user.accommodation_type == 'Single use' %>
			Late Registration: €950
		<% elsif @user.accommodation_type == 'Double use' %>
			Late Registration: €795 (per person)
		<% end %>
	<% end %>
	<% if @user.accommodation_type == 'Double use' %>
	Sharing room with: <%= @user.roomie_first_name %> <%= @user.roomie_last_name %>
	<% end %>

	<% if @user.extra_accommodation %>
	Extra accommodation? <%= @user.extra_accommodation ? 'Yes': 'No'  %>
	Check-in: <%= l @user.check_in, :format => :long %>
	Check-out: <%= l @user.check_out, :format => :long %>
	<% end %>

	Accompanying guest? <%= @user.has_guest ? 'Yes': 'No'  %>
	<% if @user.has_guest %>
	<% if @user.guest_supplement_HB %>
	Accommodation Supplement (Hotel + Breakfast): €207
	<% end %>
	<% if @user.guest_supplement_HBD %>
	Accommodation Supplement (Hotel + Breakfast + Dinner): €327
	<% end %>
	<% if @user.guest_trip %>
	Trip to Segovia (with Dinner): €63
	<% end %>
	<% end %>

	<% if !@user.comment.empty?%>
	Comments:
	<%= @user.comment %>
	<% end %>

	Total amount due: <%= @user.payment_due %>

To complete your registration please follow the payment instructions on <%= url_for(:host => "dicty12.dictybase.org", :controller => "registration", :action => "index") %>. Following are the bank details

	Bank transfer for VIBO Viajes
    ACCOUNT NAME: VIBO Viajes
    BANK: Bankia
    SWIFT: CAHMESMMXXX
    IBAN ES94 2038 8835 55 6000021158
    VAT nº C.I.F. A-07001415
    Address: C/General Tovar, 3, 46003 Valencia, SPAIN

Your registration will not be considered unless a payment confirmation is received.

Thank you.

*Please do not reply to this email.
